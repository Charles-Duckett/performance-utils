from setuptools import setup, find_packages

setup(
    name="my_package",
    version="0.1",
    author="Chase Duckett",
    description="A package for getting the most out of my computer",
    packages=find_packages(),
    install_requires=[],
)
